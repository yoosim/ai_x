JavaScript 
ch06_객체

객체에는 메소드 

1. 객체
for-in반복문
for (let idx in arr){ }

toString()
 객체를 문자열로 변환할 때 자동으로 호출되는 메서드

객체 속성 : 값 / 기능(메소드) 가능

객체배열 :

객체생성함수 ->생성자 함수 ->class

function 생성자함수명(속성값들) {
            ...
       }
       var 객체명 = new 생성자함수명(속성값들); 
